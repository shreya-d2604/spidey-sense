echo '# 1. break it — query guaranteed to return zero issues
bdata scraper run c_mt35mnl52ir8v9u3z7 "https://github.com/bpfman/bpfman-operator/issues?q=is:open+is:issue+label:%22good+first+issue%22+label:%22this-label-does-not-exist-xyz123%22" -o data/bpfman.json

# 2. validate — should fail
python3 scripts/validate.py data/bpfman.json

# 3. heal it
bdata scraper heal c_mt35mnl52ir8v9u3z7 "The scrape came back with zero issues even though the good-first-issue label has open issues on it. Please check the extraction and query handling." --auto-approve

# 4. run against the real url again
bdata scraper run c_mt35mnl52ir8v9u3z7 "https://github.com/bpfman/bpfman-operator/issues?q=is:open+is:issue+label:%22good+first+issue%22" -o data/bpfman.json

# 5. clean + validate — should pass
python3 scripts/clean.py data/bpfman.json
python3 scripts/validate.py data/bpfman.json'
